console.log("linked");


$(document).ready(onReady);

function Onready() {
  $('#addingButton').on('click', addingButton);
  $('#subtractingButton').on('click', subtractingButton);
  $('#multiplyingButton').on('click', multiplyingButton);
  $('#dividingButton').on('click',multiplyingButton);
}

function addingButton() {
  
}
